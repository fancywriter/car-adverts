# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

GET     /adverts                    controllers.Application.getAdverts(sort: String ?= "id")
GET     /adverts/:id                controllers.Application.getAdvert(id: java.util.UUID)
POST    /adverts                    controllers.Application.createAdvert()
PUT     /adverts/:id                controllers.Application.modifyAdvert(id: java.util.UUID)
DELETE  /adverts/:id                controllers.Application.deleteAdvert(id: java.util.UUID)

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)
